package org.example.projectjavafx;

import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.control.RadioButton;
import javafx.scene.control.ToggleGroup;
import javafx.scene.image.Image;
import javafx.scene.image.ImageView;

public class AvatarPageController {
   /* @FXML
    private Label welcomeText;
    @FXML
    private Button goBackButton;

    @FXML
    ToggleGroup avatar = new ToggleGroup();


    public void initialize() {
        avatar = new ToggleGroup();
        avatar1.setToggleGroup(avatar);
        avatar2.setToggleGroup(avatar);
        avatar3.setToggleGroup(avatar);
        avatar4.setToggleGroup(avatar);
        avatar5.setToggleGroup(avatar);
        avatar6.setToggleGroup(avatar);



    RadioButton avatar1 = createRadioButtonWithImage("avatar (8).png", avatar) ;
    RadioButton avatar2 = createRadioButtonWithImage("avatar (7).png", avatar);
    RadioButton avatar3 = createRadioButtonWithImage("avatar (6).png", avatar);
    RadioButton avatar4 = createRadioButtonWithImage("avatar (5).png", avatar);
    RadioButton avatar5 =  createRadioButtonWithImage("avatar (4).png", avatar);
    RadioButton avatar6 = createRadioButtonWithImage("avatar (3).png", avatar);


    @FXML
    void goBackButtonOnAction(ActionEvent event) {
        
    }

    //this method creates radiobuttons that are bounded to the images and sets their toggleGroups
    public RadioButton createRadioButtonWithImage(String imagePath, ToggleGroup avatar)
    {
        ImageView imageView = new ImageView(new Image(imagePath));
        imageView.setFitWidth(80);
        imageView.setFitHeight(80);

        //bounding the avatar images to the radio buttons
        RadioButton button = new RadioButton();
        button.setToggleGroup(avatar);
        button.setGraphic(imageView);
        
        return button;
    }


}

    //this method will handle changing the profile picture when selected
    @FXML
    void getProfilePicture(ActionEvent event)
    {
        if(avatar1.isSelected()) {}
        else if (avatar2.isSelected()) {}
        else if (avatar3.isSelected()) {}
        else if (avatar4.isSelected()) {}
        else if (avatar5.isSelected()) {}
        else if (avatar6.isSelected()) {}
    }*/
    
    }

